modLoader="javafml" #mandatory
loaderVersion="[39,)" #mandatory This is typically bumped every Minecraft version by Forge. See our download page for lists of versions.
license="MIT"
[[mods]] #mandatory
modId="beyond_earth_extras" #mandatory
version="${file.jarVersion}" #mandatory
displayName="Beyond Earth : Extras" #mandatory
logoFile="beyond_earth_extras.png" #optional
credits="" #optional
authors="gisellevonbingen" #optional
description='''
Beyond Earth's addon mod about Space Station
'''
# A dependency - use the . to indicate dependency for a specific modid. Dependencies are optional.
[[dependencies.beyond_earth_extras]] #optional
    # the modid of the dependency
    modId="forge" #mandatory
    # Does this dependency have to exist - if not, ordering below must be specified
    mandatory=true #mandatory
    # The version range of the dependency
    versionRange="[39,)" #mandatory
    # An ordering relationship for the dependency - BEFORE or AFTER required if the relationship is not mandatory
    ordering="NONE"
    # Side this dependency is applied on - BOTH, CLIENT or SERVER
    side="BOTH"
# Here's another dependency
[[dependencies.beyond_earth_extras]]
    modId="minecraft"
    mandatory=true
# This version range declares a minimum of the current minecraft version up to but not including the next major version
    versionRange="[1.18.1,1.19)"
    ordering="NONE"
    side="BOTH"
[[dependencies.beyond_earth_extras]]
    modId="beyond_earth"
    mandatory=true
    versionRange="[6.0,)"
    ordering="AFTER"
    side="BOTH"
